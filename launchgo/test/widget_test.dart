import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // This project integrates platform plugins (Firebase, CallKit, Stream) and a
    // provider-based app bootstrap; the default counter template test does not apply.
    expect(true, isTrue);
  });
}
